import { createContext } from "react"; //createcontext which is used to update to the components easily without any headache and long process.

const noteContext=createContext();

export default noteContext;